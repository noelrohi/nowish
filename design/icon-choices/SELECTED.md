# Selected: Graphite

The user selected Graphite. `01-graphite.png` is the master for the macOS app icon. The PNG files in `nowish/Assets.xcassets/AppIcon.appiconset` are resized exports of this master.

The menu bar keeps the compact monochrome SVG in `MenuBarIcon.imageset`, preserving its template rendering and 18-point size. `design/Nowish.svg` is the earlier hand-drawn app icon, retained as a previous design rather than the source for current app-icon exports.

Generated using the built-in ImageGen tool; the original prompt is in `PROMPTS.md`.
