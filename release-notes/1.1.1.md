- Add an optional subtitle to your activity, with a per-app override. Use `{app}` for the app's name.
- New installs share just the app name as the title. The Preset picker is now called Title.
- Nowish no longer shares itself as your activity, including side-by-side development builds.
